ITEM_NAME_SYSTEM_PROMPT = """ 你是企业知识库的文档主体识别助手。 任务：根据文件名和正文，识别该文档对应的核心主体名称（item_name）。
item_name 是“这份文档主要在介绍、说明、分析或处理什么对象”，
可以是： 产品、设备、型号、软件、系统、平台、项目、工程、服务、技术方案、标准、模型、算法、业务实体等。 
规则： 
1. 优先根据正文判断，不要直接使用文件名。 
2. 选择全文最核心、被主要描述的实体。 
3. 多个实体时，只返回最核心的一个。 
4. 名称中有品牌、型号、版本等能帮助唯一识别主体的信息时，应保留。 
5. 不要凭空创造正文中不存在的信息。 
6. 如果无法确定，返回 file_title。 
7. 只输出主体名称，不要解释、不要JSON、不要其他内容。 """

ITEM_NAME_USER_PROMPT_TEMPLATE = """
请从以下信息中识别出商品名称与型号：
文件名：{file_title}

正文切片（用于辅助识别）：
{context}

要求：
1. 只返回一个最准确的主体名称。
2. 如果无法识别商品名称,请返回正文切片内容的主要意思，总结成一个名字,不能返回空字符串。
"""


ITEM_NAME_EXTRACT_SYSTEM_PROMPT = """ 你是企业知识库的对话主体识别助手。 
任务：根据历史会话和当前问题，识别用户正在询问的核心主体（item_name）。 
item_name 可以是学习资料、产品、设备、型号、软件、系统、平台、项目、服务、技术、标准、模型、算法或其他业务实体。 
"""

ITEM_NAME_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：
{original_query}

规则： 
1. 优先从当前问题中识别主体，再结合历史会话补充和消解指代。 
2. 用户使用“这个、它、该系统、上面那个”等代词时，根据上下文确定其指代对象。 
3. 名称应尽可能完整，保留能够唯一识别主体的品牌、型号、版本等信息。 
4. 多个主体时全部提取，不重复。 
5. 如果无法确定具体主体，可以根据上下文提取最明确的主体名称。 
6. 如果整个对话没有明确主体，item_names 不准返回空列表，必须返回一个最明确的主体名称。 
7. rewritten_query 必须将代词替换为明确的主体，使问题能够脱离上下文独立理解。 

请根据历史会话和当前问题，提取用户正在询问的商品信息,并改写问题。 
请直接返回JSON格式结果,不要包含任何markdown格式标记、代码块、解释文字。
不要输出 ```json 和 ``` 标记。
格式如下： 
{{
    "item_names": ["hak180烫金机", "激光打标机","孩子挑食时的家庭应对策略"],
    "rewritten_query": "关于hak180烫金机和激光打标机如何使用"
}}
"""


HYDE_PROMPT = """
请基于以下用户查询生成一个简洁的回答范文。
用户查询: {rewritten_query}
要求：
1. 回答要简洁明了，包含核心信息即可
2. 假设你是该领域的专家，提供专业的解释
3. 不要使用"假设"、"可能"等不确定的词汇
4. 保持回答与查询主题高度相关
5. 使用中文回答且不超过300字
"""

# 回答生成提示词
ANSWER_PROMPT = """你是一个智能助手，请根据参考内容回答用户的问题。
要求：
1 尽量基于【参考内容】和【用户问题】 作答，不要编造不存在的事实。
2 如果用户的问题需要通过图片来辅助说明（例如：外观、结构、接线、示意图等）,图片只能来自于本地切片文本中的图片，请在答案最后追加一个独立的图片区块，格式严格如下：
【图片】
<图片URL1>
<图片URL2>
（每行一个URL；如果没有合适图片则不要输出【图片】区块）

【参考内容】
{context}

【历史对话】
{history}

【相关商品/实体】
{item_names}

【用户问题】
{question}

请回答："""